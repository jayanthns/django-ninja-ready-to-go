from django.urls import path

from . import views

app_name = "ping_app_v1"

urlpatterns = [
    # This file is not used since we're using Django Ninja routers
    # but it's included for consistency with Django patterns
]
